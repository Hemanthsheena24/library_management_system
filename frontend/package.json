{
  "name": "library-management-frontend",
  "version": "1.0.0",
  "description": "Library Management System Frontend",
  "main": "index.html",
  "scripts": {
    "start": "http-server -p 8080 --cors",
    "dev": "http-server -p 8080 --cors -c-1",
    "build": "echo 'Frontend is static, no build needed'"
  },
  "keywords": [
    "library",
    "management",
    "frontend"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
